# Base URL of the .NET ScraperApi.
# Local dev: defaults to http://localhost:8080 (where dotnet run / Rider listens).
# Prod (in container): set via --build-arg VITE_API_URL=http://<scraper-host>:8080.
VITE_API_URL=http://localhost:8080
